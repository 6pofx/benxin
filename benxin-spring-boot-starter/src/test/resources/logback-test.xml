<?xml version="1.0" encoding="UTF-8"?>
<!--
  测试期日志配置：默认只留 WARN，避免成百上千条框架调试日志淹没断言失败信息。
  需要排查某个模块时，把对应 logger 调成 DEBUG 即可。
-->
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level %-40.40logger{0} : %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <logger name="com.benxin" level="WARN"/>

    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>